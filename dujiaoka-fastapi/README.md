# 🌟 小申交流站

> 分享学习，共同成长

基于 **FastAPI + Vue3** 的现代化资源分享平台，界面简洁青春，功能完善，开箱即用。

## ✨ 特性

- 🎨 **青春简洁** - 现代化 UI 设计，清新活泼的视觉体验
- 🔐 **安全可靠** - JWT 认证，权限管理，数据加密
- 📦 **功能完整** - 用户系统、资源管理、订单处理、余额支付
- 🚀 **高性能** - 异步架构，响应迅速
- 🐳 **易部署** - Docker 一键部署

## 🛠️ 技术栈

| 层级 | 技术 |
|------|------|
| **后端** | Python 3.12+, FastAPI, SQLAlchemy, Pydantic |
| **前端** | Vue 3, TypeScript, Element Plus, Vite |
| **数据库** | SQLite / PostgreSQL |
| **部署** | Docker, Nginx |

## 🚀 快速开始

### 本地开发

```bash
# 后端
cd backend
pip install -r requirements.txt
python init_data.py  # 初始化数据
python main.py       # 启动 (端口 8000)

# 前端
cd frontend
npm install
npm run dev          # 启动 (端口 3000)
```

### Docker 部署

```bash
# 1. 配置环境变量
cp env.example .env
# 编辑 .env 修改 SECRET_KEY 等配置

# 2. 启动服务
docker-compose up -d --build

# 3. 访问
# 前端: http://localhost
# API: http://localhost:8000/docs
```

### 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 用户 | user1 | user123 |

## 📁 项目结构

```
├── backend/                 # FastAPI 后端
│   ├── app/
│   │   ├── api/            # API 路由
│   │   ├── core/           # 配置、安全
│   │   ├── models/         # 数据模型
│   │   ├── schemas/        # Pydantic 验证
│   │   └── services/       # 业务逻辑
│   ├── main.py             # 入口
│   └── Dockerfile
│
├── frontend/               # Vue3 前端
│   ├── src/
│   │   ├── views/          # 页面
│   │   ├── components/     # 组件
│   │   ├── stores/         # 状态管理
│   │   └── api/            # API 封装
│   └── Dockerfile
│
├── docker-compose.yml      # Docker 编排
└── env.example             # 环境变量模板
```

## 📖 API 文档

启动后访问: http://localhost:8000/docs

## 🔧 生产部署

1. 修改 `SECRET_KEY` 为强随机值
2. 设置 `DEBUG=false`
3. 配置 PostgreSQL 数据库
4. 配置 HTTPS

## 📄 许可证

MIT License

---

💖 **小申交流站** - 分享学习，共同成长
